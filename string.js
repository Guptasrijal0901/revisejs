// declare two variables 
let a= 10;
let b=46;
// now diddrent operation we have 
// if we want to add two numbers 
console.log ("addition of two number", a+b );
// now we have to subtract two numbers 
console.log ("substraction of two number", a-b );
// now we have to multiply two numbers 
console.log ("multiplication of two number", a*b );
// now we have to divide two numbers 
console.log ("division of two number", a/b );
// if we give srting to add then 
let c="srijal"
let d=" gupta"
console.log(c+d);

